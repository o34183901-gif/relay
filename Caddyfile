# Caddy автоматически получает и продлевает TLS-сертификат Let's Encrypt для
# RELAY_HOST и проксирует шифрованный WebSocket (wss://) на релей внутри сети.
{$RELAY_HOST} {
	reverse_proxy relay:8787
}
