'use strict';

/**
 * releaseKey.js — открытая половина ключа выпуска, для релея (ВЫП-8).
 *
 * ЗАЧЕМ ОНА ЗДЕСЬ
 *
 * Релей сам забирает выпуск из репозитория раздачи: качает манифест, а по
 * адресу из манифеста — файл, и кладёт себе на диск. Значит он принимает
 * решение на основании документа, приехавшего из сети.
 *
 * Без проверки подписи это означало бы: подставь кто-нибудь свой манифест — и
 * релей пойдёт по указанному в нём адресу, куда бы тот ни вёл, и запишет что
 * дадут. Приложение подделку отвергнет (ключ вшит в APK), но узел к тому
 * времени уже сходит по чужому адресу и займёт диск. Поэтому релей проверяет
 * подпись до того, как что-либо качать.
 *
 * ПОЧЕМУ ЭТО ОТДЕЛЬНЫЙ ФАЙЛ, А НЕ ЧТЕНИЕ src/config.js
 *
 * Код релея уезжает в отдельный репозиторий ПЛОСКИМ списком файлов из server/
 * (.github/workflows/sync-relay.yml). Никакого src/ рядом с ним нет, и обращение
 * к нему в образе не разрешилось бы вовсе.
 *
 * ЧТО ДЕЛАТЬ ПРИ СМЕНЕ КЛЮЧА
 *
 * Править ОБА места — здесь и UPDATE_RELEASE_KEY в src/config.js. Расхождение
 * ловит test/releaseKeyMirror.test.js: без него релей перестал бы принимать
 * настоящие выпуски, и понять почему было бы нечем — узел просто молчит, а
 * обновления не доезжают.
 *
 * Ключ ОТКРЫТЫЙ: он и так внутри каждого APK. Секретная половина живёт только в
 * секрете сборки.
 */

const RELEASE_PUBLIC_KEY = 'jD4uopZucqULjDnHmSsS0p3I6bqw/naRBjSO8EN4Jqo=';

module.exports = { RELEASE_PUBLIC_KEY };
