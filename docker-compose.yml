# Релей «Лично» одной командой:  docker compose up -d
#
# Перед запуском скопируйте .env.example -> .env и заполните его. Caddy сам
# получит TLS-сертификат Let's Encrypt для RELAY_HOST, так что приложение сразу
# видит рабочий wss://. Звонки (TURN) включены по умолчанию — coturn поднимается
# вместе с релеем.
#
# Все сервисы имеют restart: unless-stopped и поднимаются автоматически после
# перезагрузки. watchtower авто-обновляет релей при выходе новой версии образа —
# вручную обновлять каждый сервер не нужно (см. README, раздел «Авто-обновление»).

services:
  relay:
    image: ghcr.io/o34183901-gif/relay:latest
    # Вместо готового образа можно собирать локально из репозитория, но тогда
    # авто-обновление watchtower НЕ работает (обновлять придётся git pull +
    # docker compose up -d --build):
    # build: .
    restart: unless-stopped
    labels:
      # watchtower обновляет только помеченные контейнеры (наш код)
      - com.centurylinklabs.watchtower.enable=true
    environment:
      RELAY_SELF_URL: ${RELAY_SELF_URL}
      RELAY_PEERS: ${RELAY_PEERS:-}
      TURN_SECRET: ${TURN_SECRET}
      TURN_HOST: ${TURN_HOST}
    volumes:
      - relay-data:/data
    expose:
      - "8787"

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    depends_on: [relay]
    ports:
      - "80:80"
      - "443:443"
    environment:
      RELAY_HOST: ${RELAY_HOST}
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config

  # TURN-сервер для звонков (обязателен: гарантирует соединение за строгим NAT).
  # network_mode: host — coturn слушает 3478 и диапазон 49160-49200/udp прямо на
  # хосте; эти порты нужно открыть в firewall (см. README).
  coturn:
    image: coturn/coturn:latest
    network_mode: host
    restart: unless-stopped
    command:
      - -n
      - --use-auth-secret
      - --static-auth-secret=${TURN_SECRET}
      - --realm=licno
      - --listening-port=3478
      - --external-ip=${TURN_HOST}
      - --no-tls
      - --no-dtls
      - --no-cli
      - --min-port=49160
      - --max-port=49200

  # Авто-обновление: раз в 5 минут проверяет ghcr на новую версию образа релея и
  # сам пересоздаёт контейнер (данные в volume сохраняются). Так релеи обновляются
  # сами — достаточно опубликовать новый образ (push в репозиторий relay).
  watchtower:
    image: containrrr/watchtower
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    command: --interval 300 --cleanup --label-enable

volumes:
  relay-data:
  caddy-data:
  caddy-config:
